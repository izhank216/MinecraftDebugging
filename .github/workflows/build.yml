name: build

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - uses: gradle/actions/setup-gradle@v3
      - with:
           gradle-version: 8.1
      - run: gradle build
      - uses: actions/upload-artifact@v4
        with:
          name: MinecraftDebugging
          path: build/libs/*.jar
